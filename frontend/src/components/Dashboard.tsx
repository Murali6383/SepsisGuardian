import { Activity, LogOut, ShieldCheck, Users } from 'lucide-react'; import { useNavigate } from 'react-router-dom'; import { useAuthStore } from '../store/authStore'; export default function Dashboard({ title, subtitle }: { title: string; subtitle: string }) { const nav = useNavigate(); const { user, logout } = useAuthStore(); return <main className="min-h-screen bg-slate-50"><header className="border-b bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5"><div className="flex items-center gap-3"><div className="rounded-xl bg-slate-950 p-2.5 text-white"><Activity /></div><div><b>SepsisGuardian AI</b><p className="text-xs text-slate-500">Clinical Intelligence Platform</p></div></div><button onClick={() => { logout(); nav('/login') }} className="flex gap-2 rounded-lg border px-3 py-2 text-sm font-semibold"><LogOut size={17} /> Sign out</button></div></header><section className="mx-auto max-w-7xl px-6 py-10"><div className="rounded-3xl bg-slate-950 p-8 text-white"><p className="text-xs font-bold tracking-[.2em] text-slate-500">WORKSPACE</p><h1 className="mt-3 text-3xl font-bold">{title}</h1><p className="mt-2 text-slate-400">{subtitle}</p></div><div className="mt-8 grid gap-5 md:grid-cols-3"><Card icon={<ShieldCheck />} title="Secure Access" text="JWT authentication with role-based authorization." /><Card icon={<Users />} title="User Context" text={`${user?.full_name} · ${user?.employee_id}`} /><Card icon={<Activity />} title="Next Module" text="Ready to integrate patient, nursing and AI workflows." /></div></section></main> } function Card(p: any) { return <div className="rounded-2xl border bg-white p-6 shadow-sm">{p.icon}<h3 className="mt-5 font-bold">{p.title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{p.text}</p></div> }
